const mongoose = require("mongoose")
const SupplierSchema = require('../Models/supplierSchema')

const supplierCtr    = async (req, res) => { 
    try {
   
        const  supplier = {supplierId, name, businessType, address, contactPerson, 
            phone, email, website, taxId, paymentTerms, currencyUsed, compliance, performanceMetrics } = req.body;

            
        
    } catch (error) {
        
    }
}
